{% extends 'base.html.twig' %}

{% block title %}Erreur - MiniBlog{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .error-page {
            max-width: 600px;
            margin: 0 auto;
            background-color: var(--white);
            padding: calc(var(--spacing) * 6);
            border-radius: 8px;
            box-shadow: var(--shadow);
            text-align: center;
        }
        .error-icon {
            font-size: 4rem;
            margin-bottom: calc(var(--spacing) * 3);
        }
        .error-code {
            font-size: 4rem;
            margin-bottom: calc(var(--spacing) * 2);
            color: var(--secondary-color);
            font-weight: 700;
        }
        .error-title {
            font-size: 1.75rem;
            margin-bottom: calc(var(--spacing) * 2);
            color: var(--primary-color);
            font-weight: 600;
        }
        .error-message {
            color: var(--text-light);
            margin-bottom: calc(var(--spacing) * 4);
            line-height: 1.6;
            font-size: 1.1rem;
        }
        .error-actions {
            display: flex;
            gap: calc(var(--spacing) * 2);
            justify-content: center;
            flex-wrap: wrap;
        }
        @media (max-width: 768px) {
            .error-page {
                padding: calc(var(--spacing) * 4);
            }
            .error-code {
                font-size: 3rem;
            }
            .error-title {
                font-size: 1.5rem;
            }
            .error-actions {
                flex-direction: column;
            }
            .error-actions .btn {
                width: 100%;
            }
        }
    </style>
{% endblock %}

{% block body %}
    <div class="error-page">
        <div class="error-icon">⚠️</div>
        <div class="error-code">Oups !</div>
        <h1 class="error-title">Une erreur est survenue</h1>
        <p class="error-message">Nous sommes désolés, quelque chose s'est mal passé. Nos équipes ont été notifiées et travaillent à résoudre le problème.</p>
        <div class="error-actions">
            <a href="{{ path('app_home') }}" class="btn btn-primary">Retour à l'accueil</a>
            <a href="javascript:history.back()" class="btn btn-secondary">Page précédente</a>
        </div>
    </div>
{% endblock %}
